# AR Sandbox durable progress log

Current continuation: live Rotate/Bob behavior work is on `codex/runtime-prefab-behaviors`, based on the completed room-AR checkpoint `6962eaf`. Automated and actual Windows-player checks pass; updated AR is installed, but wearer animation acceptance is pending a system microphone unmute. Historical entries retain their original state. Read [Current-Checkpoint.md](Current-Checkpoint.md) and the latest entry at the end before resuming.

## 2026-09-20 — Quest Pro prototype continuation

User priority: Quest Pro with manually configured room data; Quest 3 optional. Meta MRUK samples take precedence over further VaM investigation. Bundled prefabs only. Acceptance: load real room → spawn → revise through natural language → PC save → clear → restore in one running app.

Baseline: existing Unity 6000.6.0f1 project, Core/MRUK 205.0.0, three bundled props, stable object IDs, anchor-local transforms, PC HTTP service, desktop fixture. Earlier desktop tests passed; native Android import was blocked by Malwarebytes quarantining generated AIBlocks assemblies. User reports adding Meta Core to allow list. No security settings changed by the agent.

Initial live check: no Unity Editor process and `adb devices -l` lists no headset. Hardware tests therefore pending; recheck after build. No Unity MCP available; use Editor batch entry points and actual-player HTTP tests.

Ownership to prevent conflicts:
- MRUK agent: QuestRoomAdapter.cs, QuestBuildSetup.cs, Docs/MRUK-QuestPro.md.
- Runtime agent: SandboxData/World/App, DesktopControls, SandboxCoreChecks, Docs/Runtime-Editing.md.
- AI agent: new ai_adapter.py, test_ai_adapter.py, Docs/AI-Integration.md.
- Coordinator: PC bridge/service/UI/persistence, generated assets/settings, build/install, integration tests, this log and final handoff.

Next: verify native compilation, add state-bound natural-language proposals and offline constrained language mode when no provider is configured, test complete loop, compile/build APK, install only if a headset becomes available. Credentials must stay server-side and out of logs/project files.

### Implementation and first build attempt

- MRUK now follows the installed ImmersiveSceneDebugger's explicit `OVRScene.RequestSpaceSetup` then device V1 reload flow. Left grip + Y opens native room setup; Y retries. Stale room targets are invalidated on failure. Pro is primary; high-fidelity/depth are unnecessary.
- Runtime repairs preserve the selected object when a different one is deleted, reject invalid selections, validate a replacement world before disposing the old one, and expose room invalidation. Core suite expanded to 80 checks (source compiled; Editor run pending).
- PC service now binds proposals to the current runtime session and scene/selection revision, expires them after two minutes, and rejects stale/replayed proposals. Save/load language intents execute on PC after prior commands are acknowledged.
- AI credential discovery found no configured compatible provider in checked environment/standard configuration locations. Offline constrained language mode and mock-provider tests are being added; no live model access is claimed.
- Direct batch build found the main project already open in Unity. It exited before compiling. To preserve the open scene, native validation now uses an isolated source copy at `work/native-validation`; output APK and log target the deliverable project's Builds/Quest and Validation/quest-pro-build.log.

### Completed implementation and final evidence

- MRUK lane finished and froze QuestRoomAdapter/QuestBuildSetup. Source follows Meta's installed debugger setup/reload pattern and official MRUK sample guidance. No further VaM investigation.
- Runtime lane finished and actual Unity fixture run passed **80 checks**. Windows player rebuilt successfully from `work/command-fixture` into `outputs/AR-Sandbox/Builds/Desktop`.
- AI lane delivered `ControlService/ai_adapter.py`, 32 tests and Docs/AI-Integration.md. No compatible credentials found. Explicit offline language mode works; configured model mode has real HTTP mock transport tests, with sanitized errors and response validation. No live-provider claim.
- PC/service lane added session/revision-bound proposals, 120-second expiry, replay rejection, PC save/load language intents, current-mode status, browser Apply flow and runtime acknowledgement messages. 17 real-HTTP service tests passed. Combined final suite: **49 tests**, no failures, ResourceWarning treated as error.
- `Validation/Run-Prototype-Loop.py` exercised **41 passing checks** against the rebuilt actual Windows player and PC service: room/catalog → text spawn → twice-size → left20cm → rotate45 → second prop → delete it → save → clear → exact restore. Same application process throughout; no runtime exceptions. This is offline language and simulated room only.
- Browser validation used the actual service/player. Verified explicit desktop/offline labels, proposed spawn, Apply acknowledgement, same-ID resize and visible completion. Fixed Connect planner-status refresh and invalid-config offline fallback. Temporary service/player were stopped after checks.
- Authored Quest adapter compiled in Editor and Android branches and QuestBuildSetup compiled against installed metadata: all exit0, deprecation warnings only. `Validation/quest-source-check.txt`. This does not replace a native build.
- Native Android import again quarantined `work/native-validation/Library/Bee/artifacts/1300b0aE.dag/Meta.XR.BuildingBlocks.AIBlocks.dll` at 12:10 local; same SHA256 as earlier Android copy. Malwarebytes report: quarantine successful. `Validation/quest-pro-build.log`. No APK produced; no security changes or repeated native retries after confirmed detection.
- ADB recheck still lists no headset. No installation, room-capture interaction, passthrough validation or physical persistence test performed.

### Resume instructions for another session

Authoritative project: `outputs/AR-Sandbox`; read README.md, Validation/Prototype-Validation.md, Docs/Security-Block.md and per-lane reports. Latest priority is Quest Pro manual floor/table setup, superseding Quest 3-primary history. Preserve the open Editor and user modifications; do not regenerate scenes in an occupied session without preserving edits.

After the external security detection is resolved, close the main project's Editor and rerun Build-Quest.ps1 (native source copy was only for isolated validation). Fix concrete build errors if any, then install with Install-Quest.ps1 only on an authorized connected headset. Run the same loop on actual Quest Pro room anchors and record hardware evidence separately. If provider credentials are supplied/configured, perform a real model proposal test; never treat the offline parser as model AI. No background automation or native build is pending.

### GitHub publication — same day

The user selected the existing public repository `School-of-the-Ancients/matrix-loading-operator`. The prototype is imported at its root on `codex/ar-sandbox-prototype`; no separate AR-Sandbox repository is created. The original MIT license and Matrix operator concept are preserved. Continue source work in this Git checkout; references above to the unversioned sandbox folder are historical.

Publication omits generated SDK credentials/session IDs, local upload preferences, raw logs, caches, builds and scene saves. Workstation paths in historical reports are redacted. A serialized-reference audit found no missing local assets after the omissions. The imported service/adapter suite passed all 49 tests again. All four PowerShell entry scripts parsed successfully. Build-Desktop.ps1 is a new convenience wrapper around the existing Editor method; native Unity import/build remains subject to the previously recorded external blocker.

## 2026-09-20 — Research-informed learning layer (in progress)

PR #1 was merged into main at 7eeb9b3. Authoritative work now lives in the Git checkout, branch `codex/learning-sessions`; canonical core is a sibling checkout `sota-v2` on `codex/operator-learning-sessions`, based on 77b070d0f421746447a800580ada1cd44e32ec28. The user asked for organization/research review then the next logical layer. Attached framework PDF matches the research repository by SHA-256; see Organization-Review.md.

Decision: canonical v2 owns a durable authored Observation and Scale session using its existing LessonRuntimeService. Operator owns real room/object binding, acknowledged scale evidence, and a read-only guide. Start binds an already placed bundled block; language editing remains through the existing proposal/apply flow. Saved scenes include immutable core checkpoint references, and restore forks that checkpoint only after Unity load acknowledgment. No cloud, voice, downloadable catalog, or invented model credentials.

Ownership: core agent owns new v2 operator domain/server/tests; content agent authored the lesson and organization review; XR agent owns guide DTO/desktop/Quest presentation and adapter tests; coordinator owns Python integration/UI/runbook/full-loop tests. Base v2 suite: 84 tests passed; build and 2 frontend tests passed. Prior Operator 49 tests still pass. Guide compiled in isolated Unity fixture and 7 contract checks passed; new Windows build is in progress. Native headset build has not been retried after the recorded security detection. Headset not connected at initial check.

Remaining: finish core/adapter tests, real Node+Python+Unity roundtrip including service restart, browser verification, source-only Quest compilation, final documentation and cross-linked PRs. Keep source and hardware results separate. Do not consume usage-reset credits without explicit confirmation.

### Connected learning implementation and validation complete

- Canonical `sota-v2` change is committed at 5812d624f2ff0cabb8ddf3e914114dd9fa052523 and published as draft PR https://github.com/School-of-the-Ancients/sota-v2/pull/77. It exposes local API v1, uses the existing LessonRuntimeService, and atomically persists authored lesson snapshots, sessions, receipts and immutable checkpoint forks. 99 Node tests pass (15 new); Vite build and two frontend tests pass.
- Operator binds an existing bundled block, preserves stable request payloads on retries, forwards authoritative Unity scale evidence, and displays sourced authored guidance with learner response history. A saved scene includes the canonical checkpoint reference. Only a successful Unity load acknowledgment with a matching resulting scene can trigger the checkpoint fork. Failed/uncertain outcomes require explicit recovery and never imply success.
- 76 Python tests pass (49 existing, 27 new adapter checks). Independent review fixes cover evidence sent on wrong action types, old receipts reactivating obsolete sessions, and inconsistent durable receipt guidance. Snapshot mismatch after a successful ACK now has a regression test.
- Fresh reproducible `Build-DesktopFixture.ps1` run: 80 core checks, seven guide checks, Windows player build success. Test output is Builds/FixtureValidation/AR-Sandbox.exe; normal default is Builds/Desktop/AR-Sandbox.exe. Raw build log and generated path-bearing core JSON remain local/ignored.
- Real Node + Python + Unity player integration: 25 checks pass, including language-driven resize, prediction/observation/reasoning/reflection, checkpoint save, clear, restart both services, exact scene restoration and a new session fork preserving the later original completed record. Repeated against the fresh fixture executable. Browser flow also completed with current scale values, response history and source links visible; no browser JS errors.
- Quest authored source compiles in Editor/Android branches against installed metadata; only existing deprecation warnings. Latest ADB check found no devices. No native build retry, vendor DLL restoration, antivirus change, APK, headset installation or physical validation. No live model credentials used or usage-reset credit redeemed.

Resume from these two Git checkouts and Docs/Learning-Sessions.md. Existing app/Vite placeholders, mastery assessment integration, cloud deployment, voice and catalogs are outside this layer. Back up both core .sota-data/operator and PC ControlService/scenes for learning saves. Pending unsaved PC operations are not recovered automatically after a PC crash. New PRs should be reviewed together; core service is needed for the authored learning panel, while ordinary sandbox editing remains available without it.

### Published handoff

Both changes are published as linked draft pull requests: [canonical core #77](https://github.com/School-of-the-Ancients/sota-v2/pull/77) and [Operator #2](https://github.com/School-of-the-Ancients/matrix-loading-operator/pull/2). Operator implementation commit: `4f9b238`; canonical implementation commit: `5812d62`. No merge or cloud deployment was performed. Temporary browser/test services and players were stopped. Startup uses separate foreground core/control-service terminals and the Windows player, as documented in the runbook. The fresh fixture build and all source/check results above are complete; only the explicitly identified native hardware/model work remains untested.

### 2026-09-20 — priority correction: virtual white room first

- User explicitly selected **fully virtual white room first** and deferred education features. The earlier learning-session drafts (operator PR #2 and sota-v2 PR #77) remain separate; this work has no learning dependency. The user's preferred education reference remains sota-beta, for later work.
- Active checkout is `outputs/matrix-white-room`, branch `codex/white-room-operator`, based on `origin/main` at `7eeb9b3`. Do not resume the learning branch for this task.
- Coordinator owns virtual scene/prefab generation, fixed room adapter, desktop controls, PC panel, integration, docs and publication. Independent lanes own runtime history/commands and real-player tests; PC language/API validation and installer; standard OpenXR controls and isolated builds. Shared runtime changes are frozen and copied explicitly before final builds.
- Added fixed room `white-room-v1`, floor anchor `white-floor`, authored furniture and three primitives. Existing MRUK scenes/scripts are preserved. Per-asset default `spawnScale` makes all control paths agree on life-size furniture. Saved SceneData remains schema 1.
- Added select, duplicate, 32-snapshot undo/redo, natural-language equivalents, exact transform fields and PC history controls. New edits invalidate redo; failed commands and selection do not create history. Provider credentials are not invented.
- First desktop build passed 125 core checks; the actual PC-to-player loop caught Unity JsonUtility materializing omitted nested DTOs and rejecting legitimate select/history messages. Fixed that boundary and added wire-deserialization checks. Final build/test results are recorded below after reruns.
- Quest virtual build uses Unity OpenXR/XR Management/Input System in `.white-room-fixture/Quest`, with no Meta Core or MRUK imports. It does not modify endpoint protection or restore quarantined files. ADB currently lists no devices; no installation or hardware test has occurred.

### White-room final functional evidence and continuation

- Both final Unity targets pass **129 core checks, 0 failures**. The real Windows player passes **84 integration checks**, including actual lease expiry and restoration in a newly launched player. The PC suite passes **68 tests** with ResourceWarning treated as an error.
- Final standalone Quest APK builds successfully: `Builds/WhiteRoomQuest/MatrixOperator.apk`, ARM64, application ID `com.matt.matrixoperator.whiteroom`. It is a virtual-room artifact; the older Meta Core/MRUK build blocker is still a separate historical issue. Installer source/parser checked; no headset connected to install or run it.
- Browser QA exercised actual runtime acknowledgement, natural-language chair spawn, precise transform, duplication/history, PC save/clear/restore, restored-object selection, stale transform draft rejection and changed-request proposal invalidation. No browser errors/warnings observed. Language mode is explicitly offline; no live-provider or voice claim.
- Build script originally rejected a valid incremental desktop build because Unity retained the unchanged launcher EXE timestamp. Fixed the wrapper to use the freshly cleared attempt log, exact output success marker, process exit and nonempty artifact. Unity's actual build success and the freshly changed managed code were separately verified by the real-player regression loop.
- Current entry points: `Build-WhiteRoom.ps1`, `Start-ControlService.ps1`, `Install-WhiteRoom.ps1`; read `Docs/White-Room.md` and `Validation/White-Room-Validation.md`. Generated projects live in ignored `.white-room-fixture/Desktop` and `/Quest`; scene names are WhiteRoomDesktop/WhiteRoomQuest. Source changes belong in this checkout, then rebuild through the script to copy them into fixtures.
- Next hardware action: connect and authorize the Quest Pro, close the desktop client, run the PC service and installer, then verify floor tracking, controller mappings, tracking recovery, rendering/comfort and the same Operator loop. Keep desktop/build evidence distinct from hardware evidence. No reset credit was consumed, no recurring task was created, and no education draft was merged.
- Final visual check rendered the seven authored props through Unity's actual camera/materials in a temporary arranged gallery. Floor-contact and camera-framing checks passed; visual inspection found all props visible with expected materials and shadows. The saved empty startup scene was verified unchanged. `Validation/white-room-preview.png` is the shareable render; this is Editor evidence, not headset evidence.
- Browser-test service and headless player were stopped after verification. Temporary browser saves are outside the repository's user save directory. No background build is pending.
- Published the isolated white-room implementation as draft [operator PR #3](https://github.com/School-of-the-Ancients/matrix-loading-operator/pull/3), based directly on main. Runtime/build implementation commit: `6cd3f0e`. The earlier education drafts remain separate and unmerged.
- Final incremental Desktop build also passed the corrected wrapper, with 129 checks and the optional preview source included. No runtime changes followed the 84-check real-player loop. Windows Git Credential Manager stalled during publication; a command-scoped GitHub CLI credential helper completed the push without changing global credential configuration.

### PR #3 follow-up — main integration and recovery fix

- User returned PR #3 for follow-up. Live inspection found it marked ready, with conflicts against main `ce906bc`: PR #2 had now been merged externally. Earlier statements about the education draft being unmerged describe the initial white-room publication, not the current base. This follow-up merges main into the PR branch without merging PR #3 itself.
- Resolved the page conflict with the white-room Operator at `/` and the preserved optional learning activity/recovery UI at `/learning`. Retained main's APIs, guide DTO/bridge, tests and the more accurate lease-expiry message “command outcome unknown.” Both pages use catalog spawn sizes; the optional page identifies a white-room client correctly.
- Found a real integration defect: scene-only loads installed a learning recovery barrier even with no active lesson, so a rejected load or expired lease blocked subsequent scene edits/saves. Added two failing regressions, then limited the barrier to actual checkpoint or active-lesson transitions. Learning restore acknowledgement checks, receipts and retry/dismiss controls remain intact.
- Final merged validation: **99 Python tests**, **129 Unity checks per target**, successful Windows and ARM64 Quest rebuilds, and **95 actual-player checks**. The strengthened player runner launches the production service CLI with the learning core deliberately unavailable, verifies expected catalog503, then proves the full white-room loop, real rejected-load recovery, expired-load recovery and exact fresh-player restoration.
- Browser verification passed natural-language spawn, save/clear/restore through the default CLI bridge, the optional page/recovery link and catalog-sized furniture spawn. Owned browser-test service/player were stopped. ADB still lists no headset; no installation or physical-controller validation is claimed.
- GitHub's automated review completed without findings for the previous head `38b5628`; that review does not cover this merge/fix until a new review runs. Source, test reports and artifact metadata were updated for this follow-up.

### Quest Pro continuation — hardware gate and installed SDK review

- User requests sequential passes: actual virtual-room headset validation, real PC-side AI, then manual-room MRUK placement with the same executor and anchor-relative saves. Bundled prefabs only; voice, catalogs and lessons remain deferred. Current checkout remains `matrix-white-room`, `codex/white-room-operator`, runtime baseline `d130e9221a23f73d3bd455fe608e3f3c6a8f15f3`; PR #3 was open/mergeable at inspection.
- Rechecked the existing 40,992,075-byte Quest APK and SHA256 `4D93BDE08321D44E8A517C53144F8812FBBEA7415E87895A8726DFF8D0523331`. All seven runtime source files match the generated Quest fixture. No runtime changes or new build were needed for this preparation.
- Started the PC service on loopback port 8765; health succeeds, runtime is offline, queue is empty, planner is explicitly unconfigured/offline. No desktop player is running. Service logs are ignored under `Validation/headset-service.*.log`. Recheck service health on resume instead of assuming the process survived.
- User reports USB debugging allowed, but repeated ADB checks still list no device. Independent Windows inspection finds no present physical Quest/Android/ADB interface, no USB error device, and only the normal Oculus virtual devices. One Unity SDK ADB daemon owns port 5037; no competing daemon or configured wireless connection was found. Host is the expected HP OMEN 30L. Asked user to reconnect an awake headset directly with a USB data cable and try another existing port/cable if necessary. No driver, device, security or ADB reset was performed.
- Added `Validation/Run-Headset-Loop.py`: attach-only read-only preflight by default, explicit `--run` for the scene test, unique pretest backup, direct-executor chair edits and exact save/clear/restore, finally restore original scene. Syntax/help and pure helper checks passed. Actual no-device preflight exits blocked before mutations; no fabricated runtime snapshot, desktop fallback, parser/model call, or headset-input automation. Reports disclose the operator-confirmed bridge attribution and lack of wearer/controller validation. The test affects in-memory undo history, which scene saves cannot restore.
- Parallel source/documentation review confirmed installed Meta Core/MRUK 205.0.0. Stock cloud AI provider assets serialize keys; the typed PC planner is the current PC-only credential path. MRUK loading/geometry queries are reusable, while direct stock prefab spawning would bypass sandbox IDs/history/saves. Manual Scene V1 remains the Quest Pro route; raw camera/depth features are separate device-restricted capabilities. Detailed verified references are in `Docs/Meta-Building-Blocks-Review.md`; no new AI/MRUK runtime integration is claimed.
- Current evidence and exact resume sequence: `Validation/Quest-Pro-Session.md`. **No APK installation, headset launch, hardware edit loop, live model call, or manual-room validation has occurred.** Proceed with installation only when the device is visible and authorized; then finish pass one before provider/MRUK work. The historical Meta native-build quarantine remains unresolved and was not retried during this virtual-stage preparation.

### Same session — USB authorized and APK installed

- USB subsequently appeared as unauthorized, then authorized after the user accepted the prompt. Confirmed actual model `Quest Pro`, manufacturer `Oculus`. The existing white-room APK installed successfully with `install -r` and reverse forwarding for port 8765 succeeded.
- Installation script's implicit MAIN/LAUNCHER launch failed. Read-only package queries prove the exported UnityPlayerGameActivity exists, resolves normally, but fails `MATCH_DEFAULT_ONLY`; its filter lacks DEFAULT. An explicit-component launch command was rejected before execution by automatic approval review with only “blocked by policy.” No indirect launch retry or device-input automation was attempted. User was asked to open **Matrix Operator** manually from **Unknown Sources**.
- Latest preflight sees the authorized device but no running app PID; PC runtime remains offline. Hardware editing, visual/controller checks, live AI and MRUK remain pending. This supersedes the earlier no-device/no-install status above.
- Read-only preparation for pass two rechecked supported provider variable presence across process/user/machine environments and narrow standard Continue/OpenCode/Aider/LM Studio/Ollama configuration locations. None supplied usable provider/model access; no Ollama/LM Studio service was running. No credential values or Codex login files were read and no inference was requested. Provider access will need user-supplied PC configuration once pass one is complete.
- Independent harness review found Ctrl+C could bypass status assignment and prevent a recovery report. Fixed both initial and cleanup interruptions; `python Validation/Run-Headset-Loop.py --self-test-interrupts` passes two isolated regressions, with no ADB, HTTP, synthetic runtime snapshots or scene mutations. Python syntax and whitespace checks pass. These helper checks do not validate device execution.

### Actual Quest Pro execution — 21 checks passed

- User manually opened Matrix Operator and reported “white room is loaded.” Exact package process was running on the authorized Quest Pro; no desktop player was running. PC state was initially offline because the USB reverse mapping was empty. Reapplying only `adb reverse tcp:8765 tcp:8765` restored the actual headset snapshot immediately. No source change, reinstall or automated launch was needed.
- Read-only device preflight passed 10 checks. The connected scene contained 17 user-created objects. `python Validation/Run-Headset-Loop.py --run` then passed **21 checks, 0 failures**, at 21:02:16–21:02:22 UTC: backup, bundled chair spawn, scale/move/rotation on the same ID, PC save, clear, exact identity/anchor/transform restore, and finally all 17 original objects plus original selection restored.
- Original scene backup: `HeadsetBackup_20260920_210216_42d0e4e7`. Edited test scene: `HeadsetLoop_20260920_210216_42d0e4e7`. PC JSON saves remain local/ignored. In-memory undo history changed as documented. Reports are `Validation/headset-preflight-results.json` and `Validation/headset-loop-results.json`.
- Hardware acknowledgements are now verified. White-room visibility is user-reported; detailed physical selection, tracking, floor height and comfort are not inferred from JSON. Asked the wearer to confirm restored objects and right-trigger selection. The test used direct executor commands, no offline parser and no AI.
- Pass two now needs actual provider access. Asked which provider to configure on the PC, explicitly keeping keys out of chat. MRUK hardware work remains after live AI verification; no source/build claim is advanced for that stage.

### ChatGPT/Codex subscription mode and Desktop handoff

- The wearer confirms that objects were restored and selection works. The direct Quest loop remains **21 passed, 0 failed**; full tracking/focus, floor-height, controller mapping and comfort observations remain distinct. The installed APK is unchanged at 40,992,075 bytes, SHA256 `4D93BDE08321D44E8A517C53144F8812FBBEA7415E87895A8726DFF8D0523331`.
- User selected their ChatGPT/Codex subscription. Added the PC `codex-cli` planner and `Start-CodexControlService.ps1`; native CLI status confirmed ChatGPT sign-in. Codex manages saved authentication; no login tokens were extracted, copied to Unity or treated as API keys. Official references: [authentication](https://learn.chatgpt.com/docs/auth) and [noninteractive mode](https://learn.chatgpt.com/docs/non-interactive-mode).
- **136 Python tests passed** in `Validation/codex-service-tests.txt`. The live Codex/headset harness completed a reviewed chair spawn with real runtime acknowledgement, then the resize proposal returned HTTP 409: **27 passed checks, 1 failure**. Recovery restored all three original objects and retained unique backup/recovery saves. Report: `Validation/codex-headset-loop-results.json`.
- Recovery showed the new test chair at yaw 45 although the verified spawn requested yaw 0; originals, catalog and anchors were unchanged. This supports stale-scene rejection, but the error body was not captured and the rotation source is unknown. The exact cause remains unconfirmed. No full live AI loop, HTTP-provider hardware loop or MRUK hardware success is claimed; no further model calls were made.
- Added `Connect-QuestControl.ps1` for USB reconnection without reinstalling/relaunching. It pins the selected authorized Quest, checks its model and PC health, restores only the selected port rule and verifies it. One actual Quest Pro run succeeded on port 8765 while the service reported online.
- Created **Desktop → Game Design → Matrix White Room Quest** and **Matrix White Room Desktop** source exports. All 103 Quest and 90 Desktop files match the generated sources by SHA-256; Unity 6000.6.0f1, scene paths, metadata and compiler defines are preserved. No Unity import/build ran at those Desktop paths. `Validation/unity-hub-export.json` records the boundary.
- The sibling **Matrix Loading Operator** full Git checkout and 255 local build files plus six local saves were also copied and hash-verified. Builds/saves remain excluded from Git; future changes in the running original service do not synchronize automatically. Read [the Desktop guide](Desktop-Unity-Hub.md) before switching service locations. Publication and checkout of the follow-up branch complete the handoff separately.
- Read-only MRUK review found the existing manual Scene V1/UUID placement and atomic wrong-room/missing-anchor load checks reusable. Minimum later changes are sharing all seven bundled props and bringing MR controller gestures/focus guards in line with the white room. No new native Meta build, antivirus change, vendor patch or quarantined-binary execution was performed. Finish the controlled live AI loop before that stage.

### Publication and Desktop checkout verified

- Committed the implementation and evidence as `d21203d4621020b89b12fdd64a345164dc20cadd` and pushed `codex/quest-pro-codex-ai`. Opened [draft PR #4](https://github.com/School-of-the-Ancients/matrix-loading-operator/pull/4), continuing the already merged PR #3. No merge of this follow-up was performed.
- The standalone Desktop `Game Design/Matrix Loading Operator` clone was switched to the new branch and verified against the same remote commit, with 186 tracked files and a clean working tree. The two Unity Hub exports and local build/save copies remain alongside it. The original service process was left running; future local saves are not synchronized between checkouts.
- This publication adds no new runtime or inference test result. Remaining work is the complete live AI loop with concurrent edits paused, then native MRUK/manual-room validation after the documented security blocker is resolved.

### Live Codex and Quest loop completed — 61 checks passed

- At 21:29:28.530–21:30:15.351 UTC, the actual Quest Pro package (PID 10902) passed **61 checks, 0 failures** in `Validation/codex-headset-loop-success.json`. Five real Codex turns completed reviewed chair spawn, double-scale edit on the same object ID, named PC save, clear and exact restore. Every model turn reported zero tool calls; runtime changes used normal Apply and headset acknowledgements.
- The pretest scene contained one table. The harness restored that table and its original selection. Backup: `CodexBackup_20260920_212928_4798309c`; edited test save: `CodexLoop_20260920_212928_4798309c`. Undo history remains changed as documented. The earlier 27/1 report is retained unchanged as the first attempt.
- The wearer confirmed: “Yes, I saw the AI changes and my scene is restored.” This establishes reported visual confirmation of the AI loop, separate from the full tracking/focus, floor-height, controller mapping and comfort checklist. MRUK/manual-room hardware validation remains pending behind the documented native-build security blocker.
- No C# or PC source changes, APK rebuild or unit-test reruns were needed; the installed APK and earlier 136-test evidence remain unchanged. The browser was showing an older offline-only page until refreshed. It now shows **Codex (ChatGPT subscription)** selected; further UI control stopped when the user began typing.
- AI workflow: refresh the Operator page → choose Codex → enter request → Create proposal → review → Apply → wait for acknowledgement. Keep controllers and other browser edits idle during inference. PR #4 is now merged; this validation-only follow-up targets `codex/quest-pro-ai-validation`.

### General prefab composition and viewer-relative requests

- Reproduced `create a room` returning HTTP 422 with the generic clearer-request message. The model contract discouraged unspecified layouts and explicitly rejected requests involving a viewer because no head pose was supplied. Empty proposals lost the model's explanation at the executor validation boundary.
- Real AI modes now reason over the existing scene and reusable prefab geometry to compose multiple final-transform spawn commands. There are no room/table/chair-arrangement recipes. Catalog entries expose cached root-local render bounds and an optional description; the bundled chair describes its facing direction. Unsupported geometry stays unknown. The model chooses reasonable defaults and reports assumptions, within the existing 20-command/100-object limits.
- Added transient viewer position/heading in available horizontal anchor frames. The Quest path requires current head tracking, floor origin and focus; the desktop camera remains usable while the browser has focus. Head movement is excluded from scene revisions, while scene/selection/catalog changes and lease changes still invalidate proposals. Saves omit viewer data. A proposal reports the exact request-time frame for inspection.
- Clarifications return a visible summary without an executable plan ID. The browser displays summary, assumptions, piece counts and progress; partial batch outcomes remain explicit. The offline parser is unchanged and is not reported as AI.
- **167 Python tests passed; 169 Unity checks passed per target; Quest ARM64 and Windows builds succeeded.** New Quest APK: 53,838,926 bytes, SHA256 `5CFBD0F64763392F9F4EC120CF8AC0358A20E84B19302DD59761021E42C1ADED`. See `Validation/composition-builds.json` and the fresh core reports. No Meta vendor assembly or antivirus settings changed.
- An isolated actual Windows player completed **90 checks, 0 failures** using two real Codex calls. The exact request `put a table in front of me with two chairs.` produced a table and two inward-facing chairs, placed from the real desktop camera frame. `create a room` produced five wall pieces with an entrance. Both arrangements passed PC save/clear/restore with exact IDs/transforms; the initial empty scene was restored. `Validation/composition-desktop-results.json` explicitly identifies desktop evidence and does not claim wearer, collision or architectural-quality validation. An initial preflight reached the first bridge poll before LateUpdate; the harness now waits briefly for initial viewpoint telemetry.
- Original headset table saved as `BeforeCompositionUpdate_20260920_154648`. ADB initially reported unauthorized; after the user accepted the prompt, `adb install -r` succeeded and the USB 8765 mapping was restored. The updated Quest app still needs to reopen before the live composition run. The PC Codex service is running on 8765; recheck live health on resume.
- Eight unchanged authored scripts were backed up and updated in each Desktop Unity Hub export. Existing user scenes/prefabs/settings were preserved; the regenerated build scenes have the new optional chair description, while existing exported scenes retain their previous catalog serialization. `Validation/composition-unity-source-sync.json` records the exact file boundary. Publication/full Desktop Git checkout synchronization follows completion of the current validation pass.

### Quest composition loop completed — 93 checks passed

- After the wearer reopened the updated app, the service received all seven measured prefab bounds and a live tracked viewer frame. The pre-update table and selection were restored before testing. `Validation/composition-headset-results.json` records **93 checks passed, 0 failed**, with two real Codex turns and actual Quest executor acknowledgements.
- The exact user request produced one table approximately 2.2 m ahead of the captured headset viewpoint and two chairs facing inward. `create a room` produced five wall pieces around the existing table, with a model-chosen 5 m square layout and 1.2 m entrance. Both used only bundled prefabs and the existing command executor. No offline parser, per-request layout template, downloaded content or generated code was used.
- Both compositions passed save → clear → restore, preserving every generated object ID, anchor and transform. Finally, the complete original table scene and object selection were restored. The saved arrangements remain available under the unique `CompositionFurniture_20260920_215516_d6b911a4` and `CompositionRoom_20260920_215516_d6b911a4` names. Viewer pose and undo history are deliberately not restored from scene saves.
- These are hardware process/acknowledgement/state results. A separate wearer visual question is pending; the harness does not certify comfort, collision clearance, room architectural quality or MRUK/passthrough behavior. The native MRUK stage remains unchanged and untested behind its documented build blocker.
- A further browser-driven real Codex request using the exact sentence showed the readable three-piece proposal, assumptions and **Apply 3 reviewed commands**. Nothing was applied during that interface check; the original table remained the sole object. The request is left in the user's Operator page for review/replanning as needed. Proposals retain their existing two-minute expiry.
- Subsequent read-only inspection observed the browser showing **Runtime confirmed all 3 commands**, four scene objects and zero pending commands. No assistant Apply action occurred after the controlled test; that newly applied arrangement is left intact. This is an additional live UI/executor result, not wearer visual confirmation.
- Published composition source/evidence as `4d15fb2` on the existing [PR #5](https://github.com/School-of-the-Ancients/matrix-loading-operator/pull/5), now titled “Compose AI layouts from prefabs and tracked Quest viewpoint.” The standalone Desktop full checkout was fast-forwarded to that implementation, preserving its six untracked Unity-generated paths. Copied and SHA-256-verified 255 build files and four unique composition/update saves to that checkout. The reusable desktop runner also passed a real 13-check preflight with no inference and verified owned-process cleanup. The original PC service remains running; future saves still do not synchronize automatically.

### Wearer confirmation and durable checkpoint

- The user confirmed **“it works”** and requested that everything be committed, pushed and checkpointed. This resolves the previously pending general visual/user confirmation. It does not add a formal comfort, collision or native MRUK test result; the detailed harness reports remain unchanged.
- Saved the current live scene through the PC service as **`MatrixCheckpoint_20260920_160737`**, with **40 objects**, and verified its saved content/hash. The scene continued to evolve after the earlier four-object browser check, so the checkpoint preserves the newer layout. No scene was cleared or replaced during checkpointing.
- [Current-Checkpoint.md](Current-Checkpoint.md) is the resume entry point, with the recovery tag, scene hash, source locations, build identity, proven behavior and pending MRUK stage. [The wearer confirmation record](../Validation/composition-wearer-confirmation.json) is separate from automated test evidence.
- Local checkpoint packaging preserves built players, personal PC saves, Unity Hub project sources and Desktop-local Unity assets alongside a Git bundle and hash inventory. These local extras remain outside public Git. The checkpoint tag is `checkpoint/2026-09-20-160737`; the authoritative branch and standalone Desktop clone are synchronized after publishing.

### Push-to-talk, selected-object speech and Codex controls

- Preserved the working `6e20899` checkpoint before implementation. Saved the current 40-object scene again as `BeforeVoice_20260920` and `BeforeVoiceInstall_20260920`; neither save contains provider credentials or live viewer pose.
- Left trigger now captures up to 15 seconds of microphone audio, with Android permission and tracking/focus cancellation. Transcription runs as bounded local English Whisper inference on the PC; its optional dependencies and pinned public model live outside Git. Real Codex receives the transcript through the existing validated proposal pipeline. Y explicitly applies a reviewed proposal; left grip undoes one edit. Text input, browser review and runtime acknowledgements remain available.
- Recording captures the complete scene, selected stable object ID and viewer pose at its start. Scene/selection changes, lease replacement or cancellation during transcription/planning prevent execution. Head movement does not retarget a captured request. The AI contract now explicitly resolves “this”, “that” and “this object” to the supplied selected ID. No offline parser is substituted for voice AI.
- The PC panel exposes Codex model and reasoning selectors from the installed CLI's safe model metadata. Supported combinations are validated before inference, remain session preferences, and apply to typed and voice requests. They do not change global Codex configuration. Request receipts distinguish requested settings from any model actually reported by the CLI.
- Reviewed Microsoft LLMR at a pinned source commit: retained our runtime command executor and borrowed no generated-code engine. The concrete context/voice gaps and source evidence are in `LLMR-Voice-Review.md`. Its commented-out Whisper example is not treated as a ready speech integration.
- **Validation:** 214 Python tests, 181 Unity core checks in each player build, browser selector interactions, and six checks against a real requested Luna/low Codex inference. A separate owned Windows player passed **24 real speech/model/executor checks**, including a table with two chairs, doubling the same selected table, undo/redo and exact PC save/clear/restore. That audio was synthesized Windows speech, not the Quest microphone. Owned test processes were stopped; personal saves and the live Quest service were not used by that harness.
- Built the updated Quest APK with `RECORD_AUDIO`, SHA-256 `915651699DFC6B96A70AF3043E128C92E2B7318EE296A8ED6025A52FC82CBDAE`, 53,846,466 bytes. After the wearer accepted the current USB authorization, `adb install -r` succeeded and USB port forwarding was restored. The device initially reported microphone permission `granted=false`; opening the app and accepting that prompt are wearer actions. New microphone capture, controller gestures and HUD comfort remain pending until separately recorded.
- Restarted the PC Codex service from the authoritative checkout. Current resume instructions are in `Voice-And-Codex-Controls.md`. Native MRUK/passthrough remains unchanged and hardware-unverified behind its documented Meta assembly blocker.
- The wearer accepted microphone permission; the device independently reported it granted. The new app connected to the PC and acknowledged restoring `BeforeVoiceInstall_20260920`; all 40 object IDs, anchors and transforms exactly matched the saved scene. A wearer voice/select/Apply/undo check is pending separately. Twelve authored script copies were verified into the two Desktop Unity Hub exports, preserving their user scenes, prefabs and settings.
- The final Python run passed **214 tests**. A preceding run had a transient Windows `10053` socket abort in the existing oversized-body HTTP test; that unchanged test then passed alone and in the complete rerun. Both run logs are retained. No assertion or production guard was weakened to obtain the pass. The setup script also completed successfully under Windows PowerShell 5.1 with the pinned dependencies and cached model.

### Actual Quest speech and selected-object editing observed

- The first live microphone request was transcribed as “make the table twice as big.” No object was selected and the scene contained two tables, so real Codex requested clarification with no executable proposal. The wearer then selected a table and spoke a larger request covering the table, chairs and tabletop object.
- The actual Quest process (observed PID 19654) sent “Make this table twice as big and the chairs and the object on the table”. Real Codex returned four validated transform commands grounded in selected table ID `eee20b8ef756428191d297c7b1bc925e`. After explicit Apply, the Quest acknowledged every command and all four objects' scales doubled. Read-only polling then observed four successive undo changes. The complete 40-object scene again exactly matched `BeforeVoiceInstall_20260920`, with zero queued commands.
- `Validation/voice-headset-results.json` records **12 passed device/network checks**, model receipt and before/after scale transitions. No microphone audio was stored. Separate wearer confirmation of physical button identities and HUD readability remains pending; no comfort, comprehensive tracking-loss or native MRUK claim is made.
- Implementation is published as `af90f35` on `codex/quest-pro-ai-validation` / PR #5. The Desktop full Git clone was fast-forwarded, 257 build/save files were copied and hash-verified, and local speech setup completed there as well. The original service remains the live process; switching checkouts requires stopping that service first. Hardware evidence and this final documentation follow in a second commit.

## 2026-09-20 — Quest Pro room-aware AR implementation

- Created `codex/quest-pro-room-ar` from the merged PR #5 baseline (`c6f9356`). Preserved the separate white-room package and saved the current 40-object layout as `BeforeRoomAR_20260920` before service changes.
- Added a separate native AR generator/build fixture using official Core/MRUK 205 packages, passthrough, Scene Model V1 and the same seven bundled prefabs. Voice, stable object selection, model/reasoning preferences, review/apply, undo and PC persistence use the existing pipeline.
- Added semantic outlines and a wearer alignment gate, measured anchor metadata/geometry/room pose, live head/controller context, and runtime surface placement using rotated/scaled prefab bounds plus actual MRUK contact queries. Missing anchors reject restores atomically. Significant room/anchor changes pause editing and retain read-only poses for save/clear/reload recovery; head/controller motion alone does not invalidate a captured request.
- Validation so far: 266 Python tests passed; native Editor compilation and 236 Unity core checks passed. Three real Codex requests using explicitly synthetic room snapshots passed table placement, ambiguity clarification and same-object movement. They did not execute on a headset. Browser checks with a separate synthetic service verified alignment gating and recovery Save/Clear availability.
- Two ordinary imports/builds were denied at Meta's generated AIBlocks DLL. The wearer reported restoring each quarantine. After the second restoration, the same native fixture passed compilation/core checks and entered Android player generation. No endpoint-protection settings or vendor packages were modified by Codex. Final APK/install and actual MRUK alignment/voice acceptance remain pending in this entry.
- Updated PC service is running from this checkout on 8765, using existing PC-only Codex sign-in and local Whisper. The prior `gpt-5.6-sol` / `xhigh` preferences were restored. Read `Room-AR.md` for the exact build, manual setup, controls and acceptance sequence. Verify live service/device state on resume.

- Native build completed successfully: `MatrixOperatorAR.apk`, 63,346,980 bytes, SHA-256 `BD83A89FEC5B4E6D966E7F3AF057CD92F0BC3B1A84954107045F6C7686850111`. APK inspection confirms package `com.matt.arsandbox`, ARM64, SDK 32/34 and the required scene/microphone/internet/passthrough declarations. ADB subsequently reported unauthorized; installation is waiting for the current wearer USB prompt. Source checkpoint `819e999` is pushed in draft PR #6. Desktop full clone is on the same branch; 14 authored source copies were updated in the two white-room Hub exports after baseline comparison, with backups and no scene/prefab/settings edits.

- Native AR installed successfully on the connected Quest Pro; both Android package IDs were independently listed afterward. The wearer reports passthrough and labeled floor/wall/table outlines. Real alignment, voice placement/movement and anchored restoration remain pending. The white-room Quest regression APK also built successfully with 236 checks; it was not installed over the working voice app. Its shutdown was followed by another unauthorized ADB state, so the wearer was asked to accept the current prompt again. The prior hardware-tested voice APK (SHA-256 `915651699DFC6B96A70AF3043E128C92E2B7318EE296A8ED6025A52FC82CBDAE`) and pre-AR 40-object save were preserved in the local sibling checkpoint `WhiteRoomVoiceBeforeRoomAR_20260920`. Native AR APK was copied and hash-verified in the Desktop full clone.

- Wearer explicitly confirms table top/edges, floor and walls align with their real positions. This is human visual evidence; no PC AR snapshot has arrived. ADB remained unauthorized even after reported prompt acceptance and a reconnect attempt. Started a persistent foreground ADB host using the existing key (no key/config changes), then asked for a fresh cable reconnect/debug prompt. Live voice orb placement/movement and table-aligned save/restore are still untested. Do not mark `confirm_room` complete before the live AR client connects. Current task-owned sessions: PC service 69850, ADB host 65613, bounded read-only/reverse-repair observer 63902. On resume, check whether these are still running before starting replacements.


### Quest Pro room AR acceptance completed — actual microphone, Codex and table anchors

- After a fresh USB reconnect and debugging approval, the persistent ADB host and USB reverse mapping remained available. The native AR app connected to the existing PC service with **28 MRUK anchors and seven supported surfaces**. The runtime acknowledged `confirm_room` after the wearer had verified the outlines against reality. No sample room or camera/depth-only feature was used.
- Actual headset speech **“Put an orb on this table”** went through the PC's local Whisper transcription and real Codex CLI provider, with requested model `gpt-5.6-sol` and reasoning `xhigh`. The reviewed surface-placement command executed against the selected physical TABLE UUID. The wearer confirmed **“The orb appears on my real table.”** Real room IDs and raw geometry remain local, outside Git.
- A subsequent 20-centimeter leftward request produced a clarification and zero commands: only approximately 2.1 cm of measured clearance remained. The orb stayed unchanged. The wearer then said **“Move the orb five centimeters to the center of the table or just to the center of the table”**. Codex explicitly interpreted the correction as the table center and proposed one `set_transform`. After Apply, the same object and same table anchor remained, position changed to the support center, and scale/rotation were preserved. The wearer confirmed movement and table alignment. This was not a five-centimeter-distance test.
- Saved through the PC service as **`RoomARAcceptance_20260920_171926`**. The on-disk scene exactly matched the live scene and excluded live headset/pointing context. The headset acknowledged Clear and reported zero objects, then acknowledged Load and restored exactly the same room ID, object ID, anchor ID and transforms. The orb was reselected afterward. The wearer confirmed **“Yes, it returned to the same spot on the real table.”** This completes the requested voice-place → voice-move → save → clear → restore acceptance loop.
- The save was copied and SHA-256 verified into the Desktop full checkout's ignored scene folder. The orb remains available in the active AR scene. The original white-room package, voice/model controls and 40-object backup remain preserved; the newly built white-room regression APK was not installed over the working voice version.
- Automated coverage remains **266 Python tests**, **236 Unity core checks in the native AR build**, **236 in the white-room Quest regression build**, and **three real Codex turns with explicitly synthetic room context**. The actual-device observations above are additional evidence, not inferred from these checks. No new source changes or test reruns were needed after the completed builds.
- Scope of hardware persistence evidence: the same running app and unchanged manual Space Setup. App restart/relocalization, recreated/deleted native anchors and physically induced tracking-loss recovery have not been exercised on hardware. Missing-anchor rejection, atomic restore and read-only recovery have automated coverage. See `Validation/room-ar-validation.json` for the sanitized acceptance record and `Room-AR.md` for the resume sequence.
- Stopped the bounded acceptance observer after completion. The PC service and existing-key ADB host are left running for continued use; verify their live state on resume. Published source/evidence goes to PR #6, with the Desktop full clone synchronized and its unrelated local Unity assets preserved.

## 2026-09-20 — Live behavior foundation and LLMR runtime review

- Started `codex/runtime-prefab-behaviors` from the completed Quest Pro room-AR checkpoint **`6962eaf`**. Saved the current scene as **`BeforeBehaviors_20260920_172929`** and preserved the working AR APK/save in the local sibling checkpoint `RoomARBeforeBehaviors_20260920`. Original white-room mode, voice, Codex model/reasoning selectors, room identity, and PC credentials remain preserved.
- Added bounded **Rotate** and upward-only **Bob** to existing bundled props. Both can coexist and independently pause/resume, disable, or be removed. Stable object/anchor IDs, configuration edits, undo/redo, legacy saves, and behavior save/load use the existing executor. A placed wrapper owns the saved baseline while its prefab child animates; animation does not churn scene revisions. Phase is not persisted, and animated swept-volume collision checks are not implemented.
- Added a shared, PC-derived **runtime skill catalog** to both real AI provider paths. It exposes only the connected player's advertised behavior kinds, with parameter units/ranges/defaults, lifecycle, and baseline ownership. Older players expose no skills; player-supplied catalog prose is discarded. Physics, triggers, paths, navigation, and arbitrary runtime code remain explicitly unsupported. The PC panel shows the selected object's configuration/status.
- The first actual desktop-player attempt exposed a Unity `JsonUtility` difference: an omitted inline behavior DTO could materialize its field-initialized defaults and be mistaken for an extra command payload. The guard now recognizes both default and all-zero absence forms while rejecting meaningful foreign behavior fields; HTTP validation still checks supplied field presence. The corrected native serialization path passed subsequent checks and player execution.
- **Validation:** 292 Python tests passed, including 26 behavior tests; 301 Unity core checks passed in each successful desktop, white-room Quest, and room-AR Quest build. An isolated actual Windows player passed **46 checks with four real Codex requests**, covering composition of behaviors, edits/lifecycle, acknowledgements, and persistence. Browser checks preserved model/reasoning preferences and verified updated controls. These are separate from headset microphone and visual evidence; see `Validation/behavior-validation.json` and `Validation/behavior-desktop-results.json`.
- Installed the updated AR build on Quest Pro. The connected runtime advertises Rotate/Bob; the wearer confirmed the room outlines still align. The original orb was restored exactly and selected. The white-room Quest regression build was not installed over its existing working application.
- **Pending hardware check:** the latest voice-behavior attempt captured no audible speech. Device audio diagnostics reported system microphone mute enabled while microphone permission was granted. The wearer was asked to unmute and repeat the request. No applied headset behavior, wearer-observed bobbing, or behavior-bearing headset save/restore is claimed at this checkpoint. Keep the existing orb and room data intact while resuming.
- Inspected Microsoft LLMR at pinned commit `9d4b72faeee99524220c31df925bcf785b272643`; the catalog adopts its skill-discovery idea without importing its generated C#/Roslyn runtime. `LLMR-Behavior-Runtime.md` maps further ideas to a **proposed** typed interpreter for reusable finite sequences, timers, interactions, procedural motion, and later bounded Unity physics bindings. Native Quest uses IL2CPP/ARM64; new data-driven combinations can avoid per-request rebuilds after their interpreter/bindings ship. That general interpreter is not implemented here.
- `GOAP-Next-Step.md` reviews the separate private playground at a pinned commit. It records the required Running/Succeeded/Failed execution seam, success-only symbolic effects, and target-invalidation replanning. No rigged character or idle/wave clips exist in that repository; character assets and room navigation remain separate later work. No GOAP source or asset packages were imported.

- Published implementation as **`12c596d`**, branch `codex/runtime-prefab-behaviors`, [draft PR #7](https://github.com/School-of-the-Ancients/matrix-loading-operator/pull/7). Full Desktop checkout/builds and both Unity exports are synchronized, preserving unrelated local Unity assets. The local `RuntimeBehaviors_20260920` checkpoint includes the source bundle, AR APK, pre-update orb save and later two-object save **`BeforeBehaviorVoice_20260920_174538`**, with verified SHA-256 inventory.
- Voice investigation update: wearer reports the Quick Settings microphone toggle was already unmuted despite Android audio diagnostics reporting system mute. Input reaches recording and app permission is allowed; no PC voice job is created. The off/on retry is pending. No audio pipeline source changes or silent-input threshold changes were made on this evidence. Do not treat the mute observation as a confirmed root cause.

- Follow-up audio evidence: explicit microphone off/on did not restore speech. A bounded read-only ADB observer captured ~5 seconds of active AR recording across 8 samples: global system mute stayed true, switch/API/restriction sources were false, and the app client reported not silenced. Recording stopped normally. This rules out an idle-only mute observation but does not identify the responsible OS/HAL policy. No audio samples were collected or settings changed by the observer. Saved the current one-orb scene as `BeforeVoiceRestart_20260920_175100` before asking for a manual app quit/reopen.

- Post-restart wearer report: "room loading voice will be available after room localization". ADB now reports unauthorized; PC is offline and its last room snapshot is stale. Asked wearer to accept the current USB prompt and look around. Code inspection also found that the voice cancellation status from `LoadRoom` is not reset on successful `BindRoom`, so that line alone cannot establish active loading. No speculative microphone fix, headset reboot success, or post-restart relocalization success is claimed. Resume by authorizing USB, running `Connect-QuestControl.ps1`, reading fresh runtime state and verifying outlines.
